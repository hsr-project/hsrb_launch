# This package has no tests
cmake_minimum_required(VERSION 3.5)
project(hsrb_gazebo_launch)
find_package(ament_cmake REQUIRED)

install(DIRECTORY launch/
  DESTINATION share/${PROJECT_NAME}/launch
)

ament_package()
